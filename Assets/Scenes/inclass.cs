using UnityEngine;
using System.Collections.Generic;

public class gjhgkh : MonoBehaviour
{
    public float floatVar = 3.5f;
    public int intVar = 10;
    public string stringVar = "hello world";

    public list<float> listVar;
    public string[] arrayDemo;

    public Gameobject cube (4);

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("konnichiwassup");   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
