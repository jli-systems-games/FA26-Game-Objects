using UnityEngine;

public class JasonExample : MonoBehaviour
{
    public float number;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log($"Number is {number}");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
